<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenue sur Madrassa{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
       <link rel="stylesheet" href="{{ asset('css/library/w3.css') }}">
          {% block stylesheets %}{% endblock %}
        <link rel="stylesheet" href= "{{ asset('css/styles.css') }}">
        <link rel="stylesheet" href= "{{ asset('css/sidenav.css') }}">
    </head>
    <body>
        {% include 'partial/sidenav-users.html.twig' %}
        <div id="main" class="w3-bar w3-border w3-black">
          <span style="font-size:30px;cursor:pointer" onclick="openNav()">&#9776; Menu</span>
            <span style="float: right">{{ app.user.firstname }} {{ app.user.lastname }}</span>
        </div>
        <div id="body" class="w3-container" style="border:1px solid red">
           {% block body %}{% endblock %}
         </div>
        {% block javascripts %}
            <script>
                  function openNav() {
                    document.getElementById("mySidenav").style.width = "250px";
                    document.getElementById("body").style.marginLeft = "250px";
                  }

                  function closeNav() {
                    document.getElementById("mySidenav").style.width = "0";
                    document.getElementById("body").style.marginLeft= "0";
                  }
            </script>
        {% endblock %}
    </body>
</html>
