{% extends 'users.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}
    <table class="w3-table w3-bordered">
        <tr>
            <th>Nom:</th><td>{{ app.user.civility }}</td>
        </tr>
        <tr>
            <th>Nom:</th><td>{{ app.user.lastname }}</td>
        </tr>
        <tr>
            <th>Prénom:</th><td>{{ app.user.firstname }}</td>
        </tr>
        <tr>
            <th>Email:</th><td>{{ app.user.email }}</td>
        </tr>
        <tr>
            <th>Téléphone:</th><td>{{ app.user.phoneMobile }}</td>
        </tr>
        <tr>
            <th>Adresse:</th><td>{{ app.user.address }}</td>
        </tr>
        <tr>
            <th>Code postale:</th><td>{{ app.user.postalCode }}</td>
        </tr>
        <tr>
            <th>Ville:</th><td>{{ app.user.city }}</td>
        </tr>
        <tr>
            <td colspan="2">
                <a href="#"><button class="w3-button w3-ripple w3-black">Modifier le profil</button></a>
                <a href="#"><button class="w3-button w3-ripple w3-black">Modifier le mot de passe</button></a>
            </td>
        </tr>
    </table>
{% endblock %}
